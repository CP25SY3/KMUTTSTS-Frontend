# utils

This folder contains general utility functions and helpers that are not specific to any feature or domain.

## Available Utils

- `mediaURL.ts` - Convert relative Strapi media URLs to absolute URLs

## Example

```
/utils/
  mediaURL.ts       // Media URL helper for Strapi
  formatCurrency.ts // Currency formatting helper
  debounce.ts       // Debounce function for performance
```
